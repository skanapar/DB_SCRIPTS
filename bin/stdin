#!/bin/env python

import sys
import os
from   sys

# Turn off buffering
sys.stdout = os.fdopen(sys.stdout.fileno(), 'w', 0)

print('stdin')

while True:
  try:
    line = raw_input()
  except:
    sys.exit()
  print(line)

